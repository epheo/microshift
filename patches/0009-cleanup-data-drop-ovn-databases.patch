diff --git a/scripts/microshift-cleanup-data.sh b/scripts/microshift-cleanup-data.sh
--- a/scripts/microshift-cleanup-data.sh
+++ b/scripts/microshift-cleanup-data.sh
@@ -127,6 +127,10 @@
         rm -rf /var/run/ovn-kubernetes
         rm -f /etc/cni/net.d/10-ovn-kubernetes.conf
         rm -f /run/cni/bin/ovn-k8s-cni-overlay
+        # The ovnkube pod keeps the OVN databases on the host under
+        # /etc/openvswitch. Left in place, a node re-roled after the wipe
+        # boots its previous logical topology into the new cluster.
+        rm -f /etc/openvswitch/ovn*_db.db /etc/openvswitch/.ovn*_db.db.~lock~
     fi
 }
 
