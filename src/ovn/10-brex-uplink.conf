# Runs after both configure-ovs scripts, so br-ex exists, and before
# microshift.service, so ovnkube-node never sees br-ex without an uplink.
[Service]
ExecStartPost=/usr/bin/microshift-brex-uplink.sh
